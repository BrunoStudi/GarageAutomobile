{% extends 'base.html.twig' %}

{% block title %}Voiture{% endblock %}

{% block body %}
    <h1>Voiture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voiture.id }}</td>
            </tr>
            <tr>
                <th>IdVoiture</th>
                <td>{{ voiture.IdVoiture }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ voiture.Prix }}</td>
            </tr>
            <tr>
                <th>ImageUrl</th>
                <td>{{ voiture.ImageUrl }}</td>
            </tr>
            <tr>
                <th>AnneeCirculation</th>
                <td>{{ voiture.AnneeCirculation ? voiture.AnneeCirculation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Kilometrage</th>
                <td>{{ voiture.Kilometrage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_voiture_index') }}">back to list</a>

    <a href="{{ path('app_voiture_edit', {'id': voiture.id}) }}">edit</a>

    {{ include('voiture/_delete_form.html.twig') }}
{% endblock %}
